// Splicing in an array is adding,removing, replacing an element to a specified place.

const Vehicals=["BMW", "FERRARI", "COROLLA", "MERCIDIES",];

Vehicals.splice (2, 2, "TOYOTA", "4*4"); // replacing the 3rd element with TOYOTA.

console.log(Vehicals);

